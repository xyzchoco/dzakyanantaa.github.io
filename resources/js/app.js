import './bootstrap';

const myTooltip = new Tooltip(document.getElementById('my-tooltip'));

// Import Tw Elements
import 'tw-elements';

// Jika Anda menggunakan vanilla JavaScript atau framework lain, pastikan untuk menginisialisasi Tw Elements dengan benar
document.addEventListener('DOMContentLoaded', () => {
  // Jika perlu, Anda bisa menambahkan konfigurasi atau inisialisasi tambahan di sini
});

